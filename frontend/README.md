# Frontend

The frontend is a static HTML, CSS, and JavaScript dashboard served by the Express backend. It includes:

- live summary metrics
- feed creation form
- topic, feed, and date filters
- responsive card grid
- server-sent events for real-time article updates

No build step is required. The backend serves this folder directly.
